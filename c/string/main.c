#include <string.h>
#include <stdlib.h>

typedef struct {
  int len;
  char* buf;
} String;

void StringAppend(String* str, const char* s) {
  if (str == NULL) return;
  if (s == NULL) return;
}

int main(int argc, char** argv) {
  return 0;
}
